# Export these in your shell; this project does not load .env files automatically.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional local Chrome executable if not using Playwright's browser installation.
# CHROMIUM_EXECUTABLE_PATH=/path/to/chrome
